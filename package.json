{
  "name": "mindgraph",
  "version": "0.2.0",
  "private": true,
  "description": "MindGraph AI graph workbench — browser-native with optional edge proxy",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs",
    "test:runtime": "node --test tests/execution-planner.test.mjs tests/plan-batch-runner.test.mjs tests/run-session-model.test.mjs",
    "start:edge": "node edge/llm-proxy.mjs"
  }
}
